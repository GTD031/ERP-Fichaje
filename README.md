# Se usó el gestor de dependencias Maven.
# El pom indica las dependencias descargadas (importante).
# Se usa mvn clean install en bash despues de clonar el proyecto (git clone ....) y se descargan las librerías automáticamente.
